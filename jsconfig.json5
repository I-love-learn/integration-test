{
  // https://zhuanlan.zhihu.com/p/55644953
  // tsconfig.json继承的编译选项 module是编译后的js模块化方式，这里为commonjs，target为es6
  "compilerOptions": {
    "module": "commonjs",
    "target": "es6",
    // baseurl 为项目根目录
    "baseUrl": ".",
    // 开启js文件中类型检查 在js语言开发的项目中启用ts类型检查 好处是有时候能避免一些类型错误，比如ui组件库的参数要传的props类型我们不知道，乱传导致的报错。
    "checkJs": true,
    // path别名
    "paths": {
      // 如果 当前打开的项目 目录不属于根目录 则可以设置这个让 / 的根目录从当前项目开始
      "@/*": ["./src/*"],
      "/*":["./*"]
    }
  },
  "exclude": ["node_modules","dist"]
}